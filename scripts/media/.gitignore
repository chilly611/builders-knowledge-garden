# Pipeline intermediates: source images, raw meshes, optimized GLBs, posters,
# prediction ids. Regenerable from objects.yaml — the library of record is
# Supabase storage (brand-assets/assets/bkg/3d/), not this repo.
work/

# Credentials. REPLICATE_API_TOKEN / SUPABASE_URL / SUPABASE_SERVICE_ROLE.
# Root .gitignore already covers .env*; repeated here so it is impossible to
# miss when reading this folder on its own.
.env
.env.*

# Committed on purpose (small jpgs, so PRs show what changed visually):
!previews/
