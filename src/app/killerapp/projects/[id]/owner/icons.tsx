/**
 * Owner Lane icons — now sourced from the shared App Shell so the chrome and
 * the Owner content share ONE icon set (moved 2026-05-31). Re-exported here so
 * existing Owner imports (`./icons`) keep working unchanged.
 */
export { Ico, StageIco } from '@/components/app-shell/icons';
